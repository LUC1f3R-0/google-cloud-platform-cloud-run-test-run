# Copy to `.env` for local runs, or set the same keys in Cloud Run (Variables & Secrets).
# Names must match what the app reads — do not rename.

PORT=8000

DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASS=
DB_NAME=

X_API_KEY=
